PORT=8080

JWT_SECRET=supersecretkey

CORS_ORIGINS=http://localhost:3000,http://localhost:8080,https://event-planner-frontend-git-crt-20226154-dev.apps.rm1.0a51.p1.openshiftapps.com/

DB_TYPE=sqlite

DB_USER=go_user
DB_PASSWORD=Creative@Password1
DB_NAME=event_planner
DB_HOST=127.0.0.1
DB_PORT=3306

DB_PATH=users.db

# ===========================================
# PRODUCTION NOTES
# ===========================================
# For OpenShift deployment:
# - Set JWT_SECRET to a secure random string
# - Set DB_TYPE=mysql
# - Configure DB_USER, DB_PASSWORD, DB_HOST, etc. for your MySQL service
# - Update CORS_ORIGINS to match your frontend domain
# - PORT will be assigned automatically by OpenShift
